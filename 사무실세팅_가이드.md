# 사무실 컴퓨터에서 Cowork로 같이 작업하기

집·사무실 두 컴퓨터에서 같은 프로젝트를 작업하기 위한 가이드.
**동기화는 Google Drive가 아니라 GitHub로 한다.** (코드 파일은 드라이브 동기화 시 복사본 충돌이 잘 남)

- GitHub 저장소: `jaemyung1287-dot/TrainingLog`
- 이 프로젝트 폴더 = 이미 위 저장소의 복제본(clone)
- push 하면 → GitHub → GitHub Actions → Firebase 자동 배포

---

## 1. 사무실 컴퓨터 최초 세팅 (한 번만)

1. **Claude 데스크톱 앱 설치**
2. **같은 계정으로 로그인** → `jaemyung1287@gmail.com`
3. Cowork에서 프로젝트 폴더 하나 생성
   - 예: `Documents/Claude/Projects/Athlete Training Log App`
4. Claude에게 요청:
   > "내 GitHub 저장소 `jaemyung1287-dot/TrainingLog`를 이 폴더로 clone 해줘"

   → 집에서 쓰던 파일, `CLAUDE.md`, 배포 설정이 그대로 복제됨.

---

## 2. 매번 작업할 때 지킬 순서 (황금 규칙)

두 컴퓨터가 안 꼬이게 하는 핵심. **순서만 지키면 됨.**

| 시점 | 할 일 | Claude에게 할 말 |
|------|-------|------------------|
| **작업 시작 전** | 최신본 받기 (pull) | "GitHub에서 최신본 받아줘" |
| 작업 | 평소처럼 수정 | (그대로 작업) |
| **작업 끝난 후** | 올리기 (push) | "수정한 거 push 해줘" |

- 작업 시작 전 **pull** → 다른 컴퓨터에서 한 작업이 반영됨
- 작업 후 **push** → Firebase 자동 배포 + 다른 컴퓨터가 받을 수 있게 됨

---

## 3. 주의사항

- ⚠️ **먼저 pull 안 하고 양쪽에서 동시에 같은 파일을 고치면 충돌(conflict)** 이 남.
  한 번에 한 컴퓨터에서만 작업하고, 끝나면 꼭 push 하는 습관을 들일 것.
- ⚠️ push 안 하고 로컬 저장만 하면 → 다른 컴퓨터·선수 링크에 **반영 안 됨.**
- 헷갈리면 그냥 Claude에게 "지금 이 폴더 GitHub랑 최신 상태 맞아?" 라고 물어보면 확인해 줌.

---

## 4. 자주 쓰는 한마디 모음

- "GitHub에서 최신본 받아줘" → 작업 시작 전
- "수정한 거 push 해줘" → 작업 끝난 후
- "지금 폴더가 GitHub 최신본이랑 같은지 확인해줘" → 헷갈릴 때
